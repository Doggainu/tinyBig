# Local dev (default)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Production (Farcaster / Base manifest asset URLs)
# NEXT_PUBLIC_SITE_URL=https://your-tinybig-app.vercel.app

# Basescan / Etherscan V2 — contract verification (works on Base chain 8453)
# ETHERSCAN_API_KEY=your_key_from_https://etherscan.io/myapikey

# Contract deploy (Foundry)
# PRIVATE_KEY=0x...                      # deployer wallet (needs ETH on Base)
# BADGE_RANK_SIGNER_PRIVATE_KEY=0x...    # rank badge signer; also used as RANK_SIGNER if unset
# RANK_SIGNER=0x...                      # optional override for BadgeNFT constructor
# Required for leaderboard on production (free tier is enough):
# BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_KEY
# or https://base.infura.io/v3/YOUR_KEY
# BASE_SEPOLIA_RPC_URL=https://sepolia.base.org

# Optional: Hub deploy block (speeds up leaderboard indexing)
# HUB_DEPLOY_FROM_BLOCK=12345678

# Base Builder Code — tags onchain txs for base.dev analytics (Settings → Builder Codes)
# NEXT_PUBLIC_BASE_BUILDER_CODE=bc_xxxxx
